const element = <h2>Well it looks like you have: Meningitis</h2>;

ReactDOM.render(
    element,
    document.getElementById("root")
);

// const domContainer = document.querySelector('#like_button_container');
// ReactDOM.render(e(LikeButton), domContainer);